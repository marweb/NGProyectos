export class Proyecto {
    id: number;
    name: string;
    image: string;
    description: string;
    stack: string;
    fecha: string;
    constructor(id: number, name: string, image: string, description: string, stack: string, fecha: number) { }
}